[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Show short test summary for all non-passing tests
addopts = -v --tb=short

# Timeout for individual tests (seconds)
# Unit/contract: fast. Integration/E2E: allow more time.
timeout = 60
